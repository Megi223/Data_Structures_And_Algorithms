#include <cmath>
#include <cstdio>
#include <vector>
#include <iostream>
#include <algorithm>
#include <unordered_map>
#include <queue>
#include <stack>
using namespace std;


int main() {
    size_t N,num;
    cin >> N;
    unordered_map<size_t,size_t> numPos;
    unordered_map<size_t,size_t> numEncounters;
    vector<size_t> nums;
    for(size_t i = 0; i< N;i++){
        cin >> num;
        numPos[num] = i;
        numEncounters[num]++;
        nums.push_back(num);
        
            
    }
    queue<size_t> ans;
    for(size_t i = 0; i< N;i++){
        if(i==numPos[nums[i]] && numEncounters[nums[i]]==1){
            cout << nums[i] << ' ';
        }
        else if(numEncounters[nums[i]]>1){
            numEncounters[nums[i]]=0;
            ans.push(nums[i]);
        }
        
    }
    while(!ans.empty()){
        cout << ans.front() << ' ';
        ans.pop();
    }
    
}
